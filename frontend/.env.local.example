NEXT_PUBLIC_API_URL=http://localhost:8000
# When accessing through a tunnel, point this at the backend's public tunnel URL instead.

# Only needed when accessing the frontend itself through a tunnel: the tunnel's
# hostname (no protocol), so the Next.js dev server accepts its requests.
# ALLOWED_DEV_ORIGINS=abc123.trycloudflare.com
